# Rules

- Keep changes small and follow existing patterns.
- Put UI colors in `src/index.css` semantic tokens, not hardcoded in TSX.
- Reuse shared UI components for repeated styles and interactions.
- Run `npm test` and `npm run build` before finishing.
